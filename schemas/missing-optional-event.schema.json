{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://platform.local/schemas/flowgrid-test/missing-optional-event.schema.json",
  "title": "FlowGrid bộ test missing optional accelerator event",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "event",
    "package",
    "runId",
    "optional",
    "reason",
    "fallback",
    "metrics"
  ],
  "properties": {
    "schemaVersion": { "const": "1.0.0" },
    "event": { "const": "flowgrid.missing-optional" },
    "package": { "const": "@platform/flowgrid-test" },
    "runId": { "type": "string", "minLength": 1 },
    "optional": { "type": "string", "minLength": 1 },
    "reason": {
      "enum": ["not-configured", "unavailable", "invocation-failed"]
    },
    "fallback": { "type": "string", "minLength": 1 },
    "metrics": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fileReads", "contextBytes"],
      "properties": {
        "fileReads": { "type": "integer", "minimum": 0 },
        "contextBytes": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
