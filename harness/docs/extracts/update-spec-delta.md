# Update spec delta (/update-spec)

Patch bundle only (delta). Emit `#update:*` tags, bump `specRevision`, run `flowgrid split` + `flowgrid check`.

Full rewrite → `/spec`. QA inbox closure → `/qa-resolve`.
