# Spec requirement (/spec)

Author **bundle YAML only** on the docs hub. Markdown under `ir/generated/` is produced by `flowgrid render`.

Path SSOT: `surfaces/<surface>/CMP-*/<NN…>/` — resolve IDs with `flowgrid_docs_route` when on a consumer repo.
